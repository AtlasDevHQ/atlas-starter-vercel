{
  "framework": "nextjs",
  "buildCommand": "bun run scripts/seed-demo.ts && bun run scripts/migrate-auth.ts && next build",
  "crons": [{ "path": "/api/v1/scheduled-tasks/tick", "schedule": "* * * * *" }]
}
